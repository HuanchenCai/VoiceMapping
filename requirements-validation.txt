# Validation / parity dependencies (Phase 0+).
# Installed in CI on top of requirements.txt. Pinned floors keep the
# reference tools reproducible across machines (conventions.md §4).

numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
soundfile>=0.10.3

# Reference tools the parity tests compare against.
praat-parselmouth>=0.4.7   # Praat bindings — jitter/shimmer/F0/HNR/formant parity
librosa>=0.10              # spectral / MFCC parity (Phase 1.8, 1.12)
opensmile>=2.4             # eGeMAPS Alpha/Hammarberg parity (Phase 1.9)

# Optional references, pulled in as later phases need them:
# nolds>=0.5.2             # Sample Entropy (CSE) reference, Phase 2
